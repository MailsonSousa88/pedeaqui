import type { LoadStoreList } from '../types/storeList'

export const loadStoreList: LoadStoreList = async () => {
  // TODO: integrar quando existir um contrato oficial de backend para a lista de lojas.
  return { status: 'notImplemented' }
}
