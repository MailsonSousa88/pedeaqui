// Spec migrado para ./__tests__/CreateStoreUseCase.spec.ts
// Este arquivo é mantido apenas para que o Jest não falhe por ausência de testes.
describe('CreateStoreUseCase (stub)', () => {
  it('spec movido para __tests__/CreateStoreUseCase.spec.ts', () => {
    expect(true).toBe(true);
  });
});
